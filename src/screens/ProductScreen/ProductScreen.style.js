import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  headerText: {
    fontSize: 20,
    textAlign: 'center',
    color: 'black',
  },
  activityIndicator: {
    marginTop: 40,
  },
});
